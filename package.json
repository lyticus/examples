{
  "name": "@lyticus/examples",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "format": "prettier --write . --ignore-path .gitignore"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
